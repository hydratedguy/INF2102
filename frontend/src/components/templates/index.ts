// Templates - Page-level layouts that define the structure
// They place organisms and molecules into a layout but don't contain business logic

export { default as SimulatorTemplate } from "./SimulatorTemplate";
